"use client";import{useState}from"react";import{dfBrowser}from"../../../lib/df-browser";export default function Login(){const[email,setEmail]=useState(""),[msg,setMsg]=useState("");async function send(e){e.preventDefault();const s=dfBrowser();if(!s){setMsg("배포 환경 연결이 필요합니다.");return}const{error}=await s.auth.signInWithOtp({email,options:{shouldCreateUser:false,emailRedirectTo:location.origin+"/focus-admin"}});setMsg(error?error.message:"로그인 링크를 이메일로 보냈습니다.")}return <main className="focusAdmin"><section className="deskHero"><small>DEVELOPMENT FOCUS</small><h1>편집국 로그인</h1><p>등록된 관리자 계정만 로그인할 수 있습니다.</p></section><form className="deskSection" onSubmit={send}><div className="deskEmpty"><b>관리자 이메일</b><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="email@example.com"/><button className="primary">로그인 링크 받기</button>{msg&&<p>{msg}</p>}</div></form></main>}