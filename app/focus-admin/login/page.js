"use client";import{useEffect,useState}from"react";import{dfBrowser}from"../../../lib/df-browser";export default function Login(){const[email,setEmail]=useState(""),[msg,setMsg]=useState("");useEffect(()=>{(async()=>{const s=dfBrowser();if(!s)return;const{data}=await s.auth.getSession();if(data.session){const{error}=await s.rpc("df_claim_admin_invite");if(!error){location.href="/focus-admin";return}setMsg(error.message)}})()},[]);async function send(e){e.preventDefault();const s=dfBrowser();if(!s){setMsg("배포 환경 연결이 필요합니다.");return}const{error}=await s.auth.signInWithOtp({email,options:{shouldCreateUser:true,emailRedirectTo:location.origin+"/focus-admin/login"}});setMsg(error?error.message:"로그인 링크를 이메일로 보냈습니다. 링크를 누르면 관리자 등록이 완료됩니다.")}return <main className="focusAdmin"><section className="deskHero"><small>DEVELOPMENT FOCUS</small><h1>편집국 로그인</h1><p>초대된 관리자 이메일만 편집국 권한을 받을 수 있습니다.</p></section><form className="deskSection" onSubmit={send}><div className="deskEmpty"><b>관리자 이메일</b><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="관리자 이메일"/><button className="primary">로그인 링크 받기</button>{msg&&<p>{msg}</p>}</div></form></main>}