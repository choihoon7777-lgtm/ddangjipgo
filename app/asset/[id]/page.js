import{assets,events}from"../../../lib/demo-data";export default async function Asset({params}){const{id}=await params;const a=assets.find(x=>x.id===id)||assets[0];return <main><a href="/">← 홈</a><div className="assetHead"><small>관심부지 · DEMO</small><h1>{a.address}</h1><span className="badge">{a.status}</span><div className="bigScore"><span>변화점수</span><b>+{a.score}</b></div></div><h2>점수 구성</h2>{events.map(e=><article key={e.id}><div className="dot">+{e.impact}</div><div><strong>{e.type} · {e.title}</strong><p>{e.summary}</p><small>신뢰도: {e.confidence}</small></div></article>)}<div className="evidence"><b>검증 원칙</b><p>사실 데이터와 AI 판단을 분리합니다. 실제 연결 후 발표기관·발표일·원문 링크를 각 이벤트에 표시합니다.</p></div></main>}