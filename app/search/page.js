"use client";import{useState}from"react";import{assets}from"../../lib/demo-data";export default function Search(){const[q,setQ]=useState("");const hit=q&&assets.filter(a=>a.address.includes(q)||q.includes("원촌"));return <main><a href="/">← 홈</a><h1>주소·지번 검색</h1><p className="muted">관심 있는 부동산은 소유 여부와 관계없이 등록할 수 있습니다.</p><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="예: 대전 유성구 원촌동 112-4"/>{hit&&hit.map(a=><a className="result" href={"/asset/"+a.id} key={a.id}><b>{a.address}</b><span>{a.type} · DEMO</span></a>)}{q&&!hit?.length&&<div className="empty">실제 주소 API 연결 전입니다. 현재는 “원촌동”을 검색해 DEMO 흐름을 확인할 수 있습니다.</div>}</main>}