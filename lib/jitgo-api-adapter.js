function makeReq(request){const u=new URL(request.url);return{method:request.method,query:Object.fromEntries(u.searchParams.entries()),headers:Object.fromEntries(request.headers.entries())}}
function makeRes(resolve){let statusCode=200;const headers={};return{setHeader(k,v){headers[k]=String(v)},status(code){statusCode=code;return this},json(body){resolve(new Response(JSON.stringify(body),{status:statusCode,headers:{"Content-Type":"application/json; charset=utf-8",...headers}}));return body}}}
export function adapt(handler,request){return new Promise(async resolve=>{const res=makeRes(resolve);try{await handler(makeReq(request),res)}catch(e){resolve(new Response(JSON.stringify({ok:false,error:"JITGO_ENGINE_ERROR",detail:String(e?.message||e)}),{status:500,headers:{"Content-Type":"application/json; charset=utf-8"}}))}})}
